
// SQL 포맷팅. 쿼리문과 매개변수(객체) 를 넣으면 해당 쿼리문이 완성된 상태로 반환됨.
// 대신 sqlString 라는 모듈을 사용하면 비슷한 결과값을 얻을 수 있음
// 현재 개발중
function sqlFormat (sql, params) {

    console.log(sql);
    console.log(params);

    return null;
}


module.exports = {
    sqlFormat
};
